{% extends 'base.html.twig' %}


{% block body %}

<a class="btn btn-back fw-bold" href="{{ path('app_portfolio_slug',{ slug: slug }) }}" role="button">Retour</a>

<div class="iframe-container">

    <iframe id="exemple création site internet" width="100%" height="100%" src="{{ url }}" >

    </iframe>
</div>

{{ slug }}


{% endblock %}