<form method="post" action="{{ path('app_categories_site_crud_delete', {'id': categories_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories_site.id) }}">
    <button class="btn">Delete</button>
</form>
